# Copy to .env and set real passwords before `docker compose up`.
# .env is read automatically by docker compose (keep it out of git).
MYSQL_ROOT_PASSWORD=change_me_root_pw
DB_USERNAME=foodapp
DB_PASSWORD=change_me_app_pw
