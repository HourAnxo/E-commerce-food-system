# Used by `npm run build`. Empty = API calls are relative (/api/...) and
# Nginx proxies them to the Spring Boot backend on the same origin.
# Set to a full URL (e.g. https://api.example.com) if the backend is on a
# different host instead.
VITE_API_URL=
